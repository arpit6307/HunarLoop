import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color primary = Colors.black;
  static const Color accent = Color(0xFFFFE600);       // Bright Yellow
  static const Color background = Color(0xFFFFE600);   // Bright Yellow Background
  static const Color cardBg = Colors.white;            // Brutalist white cards
  static const Color border = Colors.black;            // Black outlines
  static const Color warning = Colors.orange;          // Orange highlights
  static const Color textPrimary = Colors.black;       // High-contrast text
  static const Color textMuted = Color(0xFF444444);    // Muted dark text
  
  // Custom highlight cards
  static const Color highlightBlue = Color(0xFFC0E3FF);
  static const Color highlightGreen = Color(0xFFC2F1C2);
  static const Color highlightPink = Color(0xFFFFD4E2);
}
